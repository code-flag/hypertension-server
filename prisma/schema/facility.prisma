model Facility {
  id           Int             @id @default(autoincrement())
  code         String          @unique @db.VarChar(15)
  state        String?         @db.VarChar(20)
  Screening    Screening[]
}

// @Todo include Facility contact